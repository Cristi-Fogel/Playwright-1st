let message = "Hello";
message = 2;

let age = 20;
let isActive = true;
let numbers = [1,2,3];

let data = "string" + 1;
data = 1;

// functions, with error but doesn't care about types
// function addJ(a, b): {
//     return a+b;
// };
// add(3,"4");

// object example
let user = {name: "Bob", age:"34"}; //can mess around with types that are incompatible. gives errors tho
// user.location = "home"; //will compile